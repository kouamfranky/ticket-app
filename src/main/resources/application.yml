spring:
  application:
    name: ticket-app
  h2:
    console:
      enabled: true
      path: /h2-console
  datasource:
    url: jdbc:h2:mem:testdb
    driverClassName: org.h2.Driver
    username: sa
    password: Root#123*
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
server:
  port: 8085
springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html